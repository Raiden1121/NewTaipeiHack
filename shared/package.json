{
  "name": "@newtaipei-youth/shared",
  "version": "0.0.0",
  "private": true,
  "description": "Shared TypeScript types for frontend, backend, and AI service",
  "scripts": {
    "build": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  }
}
