# 複製成 ai-service/.env 後填入實際值。.env 已被 gitignore，不會進版控。
#
# 這兩個值沒設定時，ai-service 會自動使用 MockBedrockClient
# （不打任何網路、回傳標了 [mock] 的假 structured output），所以沒有 AWS 權限
# 也能跑 npm test 與 npm run dev:explain。

# 讀取順序：<repo>/.env → ai-service/.env（後者覆寫前者）。
# 隊上共用的憑證放 repo 根目錄那份就好，不需要重複一份到這裡。
#
# 相容別名（不用改隊上共用的 .env）：
#   模型     BEDROCK_MODEL_ID > MODEL_NAMME > MODEL_NAME > BEDROCK_MODEL
#   region   AWS_REGION > AWS_DEFAULT_REGION > BEDROCK_REGION
#   API key  AWS_BEARER_TOKEN_BEDROCK > BEDROCK_API_KEY > CLAUDE_KEY
# 實際採用哪個變數會印在 `npm run dev:bedrock-smoke` 的輸出裡。

# ---- 必填（要接真的 Bedrock 就這兩個）----

# Bedrock 所在的 region。要確認**這個 region 有你要用的模型**，
# 不是所有模型在所有 region 都有。
AWS_REGION=

# 模型識別碼。
#
# ⚠️ 跨區推論（cross-Region inference）的模型要用 **inference profile ID**，
#    也就是帶地理前綴的那種（us. / eu. / apac. 開頭），不是裸的 model ID。
#    用錯會拿到 ValidationException 說找不到 model identifier。
#
# 怎麼查你的帳號實際可以用哪些：
#   aws bedrock list-foundation-models --region <region> --query "modelSummaries[].modelId"
#   aws bedrock list-inference-profiles --region <region>
#
# 選型：**用 Sonnet 4.6**
#   us.anthropic.claude-sonnet-4-6
#
# 實測（npm run dev:qa-latency，只讀 analytics，含網路搜尋）：
#   純查值        10.6 秒
#   為什麼類      22.5 / 22.6 秒
#   單區小問題    25.0 秒
#   政策類        29.9 秒
#   explain       44.5 秒 ← 走預先算，線上是 5 ms
#   policyCopilot 61.9 秒 ← 走預先算，線上是 2 ms
#
# 這份檔案早期建議 Haiku 4.5，理由是「只有它進得了 API Gateway 的 30 秒上限」。
# 那個結論已經被兩件事推翻：
#   1. explain / policyCopilot 改成預先算（npm run precompute），線上不呼叫模型
#   2. Q&A 的 answer 加了 400 字上限（QA_ANSWER_MAX_CHARS）
# 而且正式路徑改成 backend 用 IAM 直接 invoke，沒有 API Gateway，30 秒上限不適用。
#
# 留 Sonnet 的理由是品質：它會主動更正使用者說錯的前提（「八里排第 4 不是第 6」）、
# 會發現兩個指標互相矛盾。那種自我檢查是小模型最容易掉的能力。
# 真的需要餘裕再換 Haiku，但換完一定要跑 npm run dev:reasoning-check。
#
# Haiku 的守規矩程度沒有退步（dev:reasoning-check 全部通過），
# 代價是分析較淺（同一請求 2–3 條結論 vs Opus 的 8–11 條）。
#
# 換模型後務必跑這兩支確認行為沒壞（不只看速度）：
#   npm run dev:reasoning-check
#   npm run dev:websearch-check
BEDROCK_MODEL_ID=

# ---- 認證：兩種方式選一種 ----

# 方式 A（預設）：AWS 憑證。sigv4 簽章，走 AWS SDK 標準憑證鏈。
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_SESSION_TOKEN=      # ASIA 開頭的臨時憑證一定要一起帶這個
#
# ⚠️ ASIA 開頭的憑證是 STS 臨時憑證，通常幾小時就過期。
#    demo 進行到一半失效是很現實的風險，過期就要重新取得。

# 方式 B：Bedrock API key（主控台產生，值以 ABSK 開頭）。
# 通常比臨時憑證活得久，臨時憑證過期時可以切過來。
# 注意：Anthropic 自家的 API key（sk-ant- 開頭）在 Bedrock 上**不能用**。
# BEDROCK_API_KEY=

# 兩種都有設定時預設用 sigv4（維持 AWS SDK 原本的優先順序）。
# 要強制走 API key 就設 bearer。
# BEDROCK_AUTH=sigv4

# ---- 選填（有預設值，通常不用動）----

# 單次回應的最大 token 數。六塊中文輸出通常 1500 以內，預設 4096 是留餘裕。
# BEDROCK_MAX_TOKENS=4096

# 0 表示盡量可重現。這是引用資料做解讀，不需要創意，所以預設 0。
# BEDROCK_TEMPERATURE=0

# 逃生門：如果選到的模型不支援 Bedrock 原生 structured outputs
# （呼叫會回 ValidationException 提到 outputConfig / schema），
# 設成 off 就改成只靠 prompt 要求 JSON，並由程式自己剖析與驗證。
# BEDROCK_STRUCTURED_OUTPUT=off

# 回應沒通過 StructuredOutputSchema 驗證時，最多呼叫幾次（含第一次）。
# BEDROCK_MAX_ATTEMPTS=2

# ---- evidence 來源 ----

# 覆寫 data-pipeline 本機輸出目錄。預設是 <repo>/data-pipeline/data。
# 產生資料的指令：
#   cd data-pipeline && python src/run_pipeline.py --period 11507 --output-dir data
# AI_DATA_DIR=
